import { AlgoConfig } from './types';

export const config = {
    defaults: {
        algo: {
            name: "test name",
            description: "test description",
            team: 1321,
            module_sources: [],
            permission_type: "can_view"
        } as AlgoConfig
    }
};